services:
  - type: web
    name: herguardian-backend
    env: node
    root: backend
    branch: main
    plan: free
    buildCommand: npm install
    startCommand: npm start
    healthCheckPath: /api/health
